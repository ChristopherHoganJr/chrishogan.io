import React, { type FC } from "react";

// components
import Navbar from "./Navbar";

const Header: FC = () => {
  return (
    <header>
      <Navbar />
    </header>
  );
};

export default Header;
